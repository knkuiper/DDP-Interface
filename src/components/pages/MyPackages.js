import React, { Fragment } from 'react';
import '../App.css';
import PackageCard from '../layouts/PackageCard';

const Packages = () => {
    return (
        <Fragment>
          <PackageCard />
        </Fragment>
    );
}

export default Packages;
